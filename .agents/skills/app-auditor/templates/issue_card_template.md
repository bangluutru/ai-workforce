#### [{severity}] {title}
* **Loại:** `{type}` (Defect / Recommendation)
* **Vị trí (Route):** `{route}`
* **Khung nhìn (Viewport):** `{viewport}`
* **Các bước tái hiện (Reproduction Steps):**
{steps}
* **Hành vi kỳ vọng (Expected):** {expected}
* **Hành vi thực tế (Actual):** {actual}
* **Bằng chứng (Evidence):**
  - Selector/Phần tử: `{selector}`
  - Ảnh chụp màn hình: `{screenshot_path}`
  - Chi tiết log/lỗi: `{log_snippet}`
